import 'package:flutter/material.dart';

import 'widgets/otpsucsessScreenBody.dart';

class otpsucsessScreen extends StatelessWidget {
  const otpsucsessScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const otpsucsessScreenBody());
  }
}
