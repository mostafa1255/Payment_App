import 'package:flutter/material.dart';

class sendButtomSheet extends StatelessWidget {
  const sendButtomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(
      onPressed: () {
        
      },
      child: Text("BE"),
    );
  }
}
